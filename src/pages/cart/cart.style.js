import styled from "styled-components";

const MainEl = styled.main `
    width: 1280px;
    margin: 150px auto 0;
    padding-bottom: 156px;
`

const Title = styled.h2 `
    font-size: 36px;
    font-weight: bold;
    text-align: center;
`

const CartListUl = styled.ul `
    margin-top: 19px;
`

export { MainEl, Title, CartListUl }